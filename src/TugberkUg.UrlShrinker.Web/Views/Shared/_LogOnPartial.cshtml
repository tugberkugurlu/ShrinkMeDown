@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log Off", "logoff", new { controller = "account" }) ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "logon", new { controller = "account" }) ]
}